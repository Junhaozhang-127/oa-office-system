<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.oa.mapper.NoticeReadMapper">

    <!-- 查询用户对指定公告的已读记录 -->
    <select id="selectByNoticeAndUser" resultType="com.buu.oa.entity.NoticeRead">
        SELECT id, notice_id, user_id, read_time, create_time
        FROM sys_notice_read
        WHERE notice_id = #{noticeId}
          AND user_id = #{userId}
        LIMIT 1
    </select>

    <!-- 查询用户已读公告ID列表 -->
    <select id="selectReadNoticeIds" resultType="long">
        SELECT notice_id
        FROM sys_notice_read
        WHERE user_id = #{userId}
          AND notice_id IN
        <foreach collection="noticeIds" item="nid" open="(" separator="," close=")">
            #{nid}
        </foreach>
    </select>

    <!-- 统计用户未读公告数量：已发布公告总数 - 已读数 -->
    <select id="countUnreadByUser" resultType="int">
        SELECT COUNT(*) - COALESCE(
            (SELECT COUNT(*) FROM sys_notice_read WHERE user_id = #{userId}), 0
        )
        FROM sys_notice
        WHERE status = 1
    </select>

</mapper>
