<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.oa.mapper.SysMenuMapper">

    <!-- 根据用户ID查询有权限的菜单，通过用户角色关联+角色菜单关联联查，去重并排序 -->
    <select id="selectByUserId" resultType="com.buu.oa.entity.SysMenu">
        SELECT DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component,
               m.perms, m.type, m.icon, m.sort, m.create_time, m.update_time
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} AND r.status = 1
        ORDER BY m.sort ASC
    </select>

    <!-- 根据角色ID列表查询菜单 -->
    <select id="selectByRoleIds" resultType="com.buu.oa.entity.SysMenu">
        SELECT DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component,
               m.perms, m.type, m.icon, m.sort, m.create_time, m.update_time
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id IN
        <foreach collection="roleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY m.sort ASC
    </select>

    <!-- 查询所有菜单（管理员） -->
    <select id="selectAllMenus" resultType="com.buu.oa.entity.SysMenu">
        SELECT id, parent_id, menu_name, path, component, perms, type, icon, sort, create_time, update_time
        FROM sys_menu
        ORDER BY sort ASC
    </select>

    <!-- 根据用户ID查询权限编码列表（仅type=2按钮权限） -->
    <select id="selectPermsByUserId" resultType="String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} AND r.status = 1 AND m.type = 2 AND m.perms != ''
    </select>

</mapper>
