<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.oa.mapper.SysUserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.buu.oa.entity.SysUser">
        SELECT id, username, password, emp_id, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <!-- 根据用户ID查询角色编码列表，联查sys_user_role + sys_role -->
    <select id="selectRoleCodesByUserId" resultType="String">
        SELECT r.role_code
        FROM sys_user_role ur
        INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} AND r.status = 1
    </select>

    <!-- 根据用户ID查询角色ID列表 -->
    <select id="selectRoleIdsByUserId" resultType="Long">
        SELECT ur.role_id
        FROM sys_user_role ur
        INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} AND r.status = 1
    </select>

</mapper>
